{% set values = manager.definition.values %}
{% block widget_content %}
    <div class="widget-content value-graph card-body w-100 h-100">
        Welcome to <b>Symfony</b><br />
        Configuration: {{ values.conf }} {% if values.scope %}({{ values.scope }}){% endif %}
    </div>
{% endblock %}
